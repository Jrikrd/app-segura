name: CI - Seguridad y Despliegue

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build_test_scan:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Instalar dependencias
        run: npm ci --legacy-peer-deps

      - name: Lint
        run: npm run lint

      - name: Pruebas
        run: npm test

      - name: Auditoría de dependencias
        run: npm run audit

      - name: Construir imagen Docker
        run: docker build -t segura-app:ci .

      - name: Arrancar app en contenedor (para ZAP)
        run: |
          docker run -d --name segura-app -p 3000:3000 --env PORT=3000 segura-app:ci
          sleep 5

      - name: Esperar a que la app responda
        run: |
          for i in {1..30}; do
            curl -fsS http://localhost:3000/health && exit 0
            sleep 2
          done
          echo "La app no respondió al /health" && exit 1

      # --- ZAP ---
      - name: Preparar carpeta de reportes ZAP
        run: |
          mkdir -p zap-reports
          chmod 777 zap-reports

      - name: OWASP ZAP baseline (docker)
        run: |
          docker run --rm \
            -v "${{ github.workspace }}/zap-reports:/zap/wrk/:rw" \
            --network=host \
            ghcr.io/zaproxy/zaproxy:stable \
            zap-baseline.py -t http://localhost:3000 \
              -J report_json.json \
              -w report_md.md \
              -r report_html.html \
              -a -m 1 || true

      - name: Listar posibles ubicaciones de reportes
        run: |
          echo "== Contenido de zap-reports/ =="
          ls -lah zap-reports || true
          echo "== Contenido de la raíz del workspace =="
          ls -lah . || true

      # Subimos primero desde zap-reports; si no hay, probamos la raíz como respaldo
      - name: Publicar reporte ZAP (carpeta dedicada)
        uses: actions/upload-artifact@v4
        with:
          name: zap-report
          path: |
            zap-reports/report_html.html
            zap-reports/report_md.md
            zap-reports/report_json.json
          if-no-files-found: warn
          retention-days: 7

      - name: Publicar reporte ZAP (fallback raíz)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: zap-report-root
          path: |
            report_html.html
            report_md.md
            report_json.json
          if-no-files-found: warn
          retention-days: 7

      - name: Parar contenedor
        if: always()
        run: docker rm -f segura-app || true