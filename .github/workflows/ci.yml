name: CI - Seguridad y Despliegue
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  build_test_scan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Instalar dependencias
        run: npm ci --legacy-peer-deps
      - name: Lint
        run: npm run lint
      - name: Pruebas
        run: npm test
      - name: Auditoría de dependencias
        run: npm run audit
      - name: Construir imagen Docker
        run: docker build -t segura-app:ci .
      - name: Arrancar app en contenedor (para ZAP)
        run: |
          docker run -d --name segura-app -p 3000:3000 --env PORT=3000 segura-app:ci
          sleep 5
      - name: OWASP ZAP baseline scan
        uses: zaproxy/action-baseline@v0.10.0
        with:
          target: 'http://localhost:3000'
          cmd_options: '-a -m 1'
      - name: Parar contenedor
        if: always()
        run: docker rm -f segura-app || true
  # Ejemplo de despliegue: ajusta a tu proveedor (Render, Railway, VM, etc.)
  # deploy:
  #   needs: build_test_scan
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Placeholder de despliegue
  #       run: echo "Implementa aquí tu comando/acción de despliegue a entorno de pruebas"
